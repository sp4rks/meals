{
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "description", "servings", "minutes", "ingredients", "steps", "notes"],
  "properties": {
    "title": {"type": "string"},
    "description": {"type": "string"},
    "servings": {"type": "integer"},
    "minutes": {"type": "integer"},
    "ingredients": {"type": "array", "description": "Ingredient names and amounts only; preparation and cooking instructions belong in steps.", "items": {"type": "string"}},
    "steps": {"type": "array", "description": "Preparation and cooking instructions.", "items": {"type": "string"}},
    "notes": {"type": "string"}
  }
}
